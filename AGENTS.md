# Agent Context

Project instructions for this repository live in **`CLAUDE.md`** in this same directory. Read it
first — the content is tool-agnostic despite the filename.

Shared personal context (global conventions, memory index, cached research) is mapped in
`~/.codex/AGENTS.md`. Read that too if you are picking up work without prior context.
